require([
    'popup'
],
/**
 * Dieses Skript übernimmt das Initialisieren der Erweiterung.
 * @exports main
 * @param {module} popup - popup-Modul
 * @see module:popup
 */
function(popup) {
    popup.init();
});