/**
 * Content Skript, das ausgeführt wird, wenn der Request Button geklickt wird.
 */
console.log("Test");


/**
    Return wert
 chrome.tabs.executeScript(tabId,
 {code:"document.charset"},
 function(results){
     // results[0] will now contain the charset for the page in question
});

 */